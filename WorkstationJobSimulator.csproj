<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <Compile Remove="TelemetryGenerator.Cli/**/*.cs" />
    <Compile Remove="TelemetryGenerator.Core/**/*.cs" />
    <Compile Remove="TelemetryGenerator.DataQualityChecker/**/*.cs" />
    <Compile Remove="AI.Training/**/*.cs" />
    <Compile Remove="PipelineRunner/**/*.cs" />
  </ItemGroup>

</Project>
